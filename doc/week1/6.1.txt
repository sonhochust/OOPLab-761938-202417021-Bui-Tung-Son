6.1:
Khi user chọn cancel có 1 hộp thoại message hiện ra với nội dung "You've chosen: No"
Nếu muốn điều chỉnh tên các nút thì sử dụng JOptionPane.showOptiondDialog
ví dụ vói yes/no:
import javax.swing.JOptionPane;

public class ChoosingOption {
    public static void main(String[] args) {
        Object[] options = {"Yes", "No"};

        int option = JOptionPane.showOptionDialog(
                null,
                "Do you want to change to the first class ticket?",
                "Ticket Confirmation",
                JOptionPane.DEFAULT_OPTION,
                JOptionPane.QUESTION_MESSAGE,
                null,
                options,
                options[0]
        );

        JOptionPane.showMessageDialog(
                null,
                "You've chosen: " + options[option]
        );
    }
}
tương tự với trường hợp i do/i don't thay vào trong mảng option

